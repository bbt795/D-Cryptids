const input_password = document.getElementById('password');
const check_pass_el = document.getElementById('checkPass');

/*
check_pass_el.addEventListener('click', async () => {
    //events to happen when clicking button
    const password = input_password.value; 


})
*/
